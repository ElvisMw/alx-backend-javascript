/**
 * Retrieves a response from the API.
 *
 * @return {Promise} A Promise that resolves with 'Response from API'
 */
export default function getResponseFromAPI() {
    return new Promise((resolve, reject) => {
        resolve('Response from API');
    });
}
